# 🌐 Database connection string
DATABASE_URL=postgresql://your_user:your_password@your_host:5432/your_database

# 🧪 Runtime environment
ENV=development

# (Optional) Secret keys or tokens (if you use them)
# SECRET_KEY=your-secret-key

# (Optional) Port for FastAPI (if not using default)
# PORT=8000
